/**
 * 获取通用商品列表配置
 * @type {{}}
 */
const searchTypeConfig = {
  0: 1,
  1: 2,
  2: 3
};

/**
 * 默认的一些静态数据
 * @type {{pageSize: number}}
 */
const staticData = {
  pageSize: 20
};

/**
 * 底部tabBar标识
 * @type {string}
 */
const symbol = 'mall';

export {
  searchTypeConfig,
  symbol,
  staticData
};
